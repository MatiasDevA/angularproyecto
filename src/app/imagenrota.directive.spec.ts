import { ImagenrotaDirective } from './imagenrota.directive';

describe('ImagenrotaDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagenrotaDirective();
    expect(directive).toBeTruthy();
  });
});
